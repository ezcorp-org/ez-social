---
phase: 03-post-queue-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/+page.server.ts
  - src/lib/components/queue/QueueTable.svelte
  - src/routes/(app)/queue/[id]/+page.svelte
  - src/routes/(app)/queue/[id]/+page.server.ts
autonomous: true
requirements: [QUEU-03, DASH-01, DASH-02]

must_haves:
  truths:
    - "User can change persona assignment on a queued post via dropdown in the table"
    - "User can archive a post from the queue table"
    - "Archived posts are hidden from default view but accessible via Archived tab"
    - "User can unarchive a post to restore it to the queue"
    - "Clicking a queue row navigates to /queue/[id] which shows a Phase 4 placeholder"
    - "Dashboard recent activity section shows last few posts added"
  artifacts:
    - path: "src/routes/(app)/queue/[id]/+page.svelte"
      provides: "Placeholder chat page for Phase 4"
      min_lines: 15
    - path: "src/routes/(app)/queue/[id]/+page.server.ts"
      provides: "Load function for single post data"
      exports: ["load"]
  key_links:
    - from: "src/routes/(app)/queue/[id]/+page.server.ts"
      to: "src/lib/server/services/queue.ts"
      via: "createQueueService getById"
      pattern: "getById"
    - from: "src/lib/components/queue/QueueTable.svelte"
      to: "?/archive"
      via: "form action for archive"
      pattern: "action.*archive"
    - from: "src/lib/components/queue/QueueTable.svelte"
      to: "?/assignPersona"
      via: "form action for persona change"
      pattern: "action.*assignPersona"
---

<objective>
Add archive management, inline persona reassignment, and the placeholder chat route.

Purpose: Complete the post queue experience with actions users need: archiving posts they don't want to reply to, changing persona assignment, and navigating to individual post pages (Phase 4 placeholder). Also refine the dashboard with the "Archived" tab and recent activity.

Output: 2 new files (placeholder chat route), 3 modified files (queue table actions, page server actions, page layout refinements).
</objective>

<execution_context>
@/home/dev/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/dev/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-post-queue-dashboard/03-CONTEXT.md
@.planning/phases/03-post-queue-dashboard/03-RESEARCH.md
@.planning/phases/03-post-queue-dashboard/03-01-SUMMARY.md
@.planning/phases/03-post-queue-dashboard/03-02-SUMMARY.md

@src/routes/(app)/+page.svelte
@src/routes/(app)/+page.server.ts
@src/lib/components/queue/QueueTable.svelte
@src/lib/server/services/queue.ts
@src/lib/schemas/queue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive and persona reassignment actions in queue table</name>
  <files>src/routes/(app)/+page.server.ts, src/lib/components/queue/QueueTable.svelte, src/routes/(app)/+page.svelte</files>
  <action>
**Add form actions to page server (`src/routes/(app)/+page.server.ts`):**

Add these form actions (the file already has `addPost`, `updateContent`, `assignPersona` from Plan 02):

`archive` action:
1. Auth check
2. Parse `postId` from formData
3. `queueService.archive(userId, postId)`
4. Return `{ success: true }`

`unarchive` action:
1. Auth check
2. Parse `postId` from formData
3. `queueService.unarchive(userId, postId)`
4. Return `{ success: true }`

**Update load function** to support an "archived" status filter:
- When `status === "archived"`, query posts where `archivedAt IS NOT NULL` instead of the normal filter
- Add this as a special case in the queue service or handle in the load function by calling a separate `listArchived(userId)` method (add to queue service if not already there)

**Update QueueTable component (`src/lib/components/queue/QueueTable.svelte`):**

Add row-level actions. Each row gets a small actions area (visible on hover or via overflow menu):

1. **Archive button**: Small icon/text button that submits a form to `?/archive` with `postId` as hidden input. Use `use:enhance` to prevent page navigation. Style as subtle gray text/icon. When in "archived" view, show "Unarchive" instead (submits to `?/unarchive`).

2. **Persona dropdown**: Inline `<select>` in the persona column. Pre-selected with current persona. On change, submit a form to `?/assignPersona` with `postId` and new `personaId`. Use `use:enhance` for seamless update. Include an "Unassigned" option with empty value.

Implementation pattern for inline form actions in table cells:
```svelte
<form method="POST" action="?/assignPersona" use:enhance={() => {
  return async ({ update }) => { await update(); };
}}>
  <input type="hidden" name="postId" value={post.id} />
  <select name="personaId" onchange={(e) => e.currentTarget.form?.requestSubmit()}>
    <option value="">Unassigned</option>
    {#each personas as persona}
      <option value={persona.id} selected={persona.id === post.personaId}>{persona.name}</option>
    {/each}
  </select>
</form>
```

QueueTable now needs `personas` prop (for the dropdown) and `showArchived` boolean prop (to toggle archive/unarchive button).

**Update StatusTabs (`src/routes/(app)/+page.svelte`):**

Add an "Archived" tab to the status tabs. It appears after "Complete" and is visually separated (maybe a `|` divider or dimmer style). When active, shows archived posts with unarchive action.

Update the `+page.svelte` to pass `showArchived={data.activeStatus === "archived"}` to QueueTable.
  </action>
  <verify>Run `bun run typecheck` — must pass. Test: (1) archive a post from the queue table — it disappears from "All" view, (2) switch to "Archived" tab — archived post appears, (3) unarchive it — it returns to the queue, (4) change persona on a post via dropdown — it updates without page reload.</verify>
  <done>Archive/unarchive works from queue table. Persona can be changed inline via dropdown. Archived tab shows archived posts with unarchive action. All actions use form actions with use:enhance (no full page reload).</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder chat route for Phase 4</name>
  <files>src/routes/(app)/queue/[id]/+page.server.ts, src/routes/(app)/queue/[id]/+page.svelte</files>
  <action>
**Page server (`src/routes/(app)/queue/[id]/+page.server.ts`):**

Load function that:
1. Gets session via `event.locals.auth()`
2. Gets DB via per-request factory
3. Gets post via `queueService.getById(userId, params.id)`
4. If post not found, throw `error(404, "Post not found")`
5. Gets the post's persona (if personaId set) via `personaService.getById(userId, post.personaId)`
6. Return `{ post, persona }`

**Page component (`src/routes/(app)/queue/[id]/+page.svelte`):**

A placeholder page that shows:
- Back link: `← Back to Dashboard` linking to `/`
- Post info card: URL (as clickable link opening in new tab), platform badge, status badge, persona name, date added
- If `postContent` exists: show the scraped content in a readable format
- If `postContent` is null: show "Content not yet available" with a manual paste form (reuse `?/updateContent` action pattern — but this action is on the dashboard page, so create a local `updateContent` action here too)
- Placeholder section: "Chat will be available here in Phase 4" styled as a muted, centered message with a subtle border. Something like:

```svelte
<div class="mt-8 rounded-lg border-2 border-dashed border-gray-200 p-12 text-center">
  <p class="text-lg font-medium text-gray-400">Chat Interface</p>
  <p class="mt-2 text-sm text-gray-400">Coming in Phase 4 — discuss this post and generate AI-powered reply drafts</p>
</div>
```

This placeholder will be replaced in Phase 4 with the actual chat interface.
  </action>
  <verify>Run `bun run typecheck` — must pass. Navigate to `/queue/[id]` (using a real post ID from the DB) and verify: (1) post details display correctly, (2) back link works, (3) Phase 4 placeholder message is visible, (4) 404 error for non-existent post ID.</verify>
  <done>Post detail page at /queue/[id] shows post info, content (or paste fallback), and Phase 4 chat placeholder. Back link navigates to dashboard. 404 for invalid post IDs. Page is ready for Phase 4 to replace the placeholder with actual chat UI.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes with 0 errors
2. Archive/unarchive cycle works correctly
3. Persona dropdown changes persist
4. "Archived" tab shows/hides archived posts
5. /queue/[id] loads post details with back navigation
6. Phase 4 placeholder is clearly visible
7. 404 for non-existent post IDs
</verification>

<success_criteria>
- User can archive a post and find it in the Archived tab
- User can unarchive a post to restore it
- User can change persona assignment via inline dropdown
- Clicking a queue row navigates to /queue/[id] with post details
- /queue/[id] shows Phase 4 placeholder and post info
- All actions use form actions with use:enhance (no full page reloads)
</success_criteria>

<output>
After completion, create `.planning/phases/03-post-queue-dashboard/03-03-SUMMARY.md`
</output>
